setResult(<%= @photo.description.to_json.html_safe %>, <%= @photo.image.url(:medium).to_json.html_safe %>);

var imaginaryWaittime = Math.floor((Math.random() * 3000) + 1000);
setTimeout(function() {
  navigate_to("results");
}, imaginaryWaittime);