setResult(<%= @photo.description%>, <%= @photo.image.url %>);

var imaginaryWaittime = Math.floor((Math.random() * 3000) + 1000);
setInterval(function() {
  setResult(title, description, image);
  navigate_to("result");
}, imaginaryWaittime);